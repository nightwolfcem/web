// app.defaults.js — detaylı .d.ts (manuel stil)
// Kaynak: core12/app.defaults.js

/**
 * defaults: app.defaults modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: app.defaults
 * @returns Record<string, any> tipinde değer.
 */
export const defaults: Record<string, any>;

