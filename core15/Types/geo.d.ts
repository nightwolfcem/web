// geo.js — detaylı .d.ts (manuel stil)
// Kaynak: core12/geo.js

/**
 * Tgeo: geo modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: geo
 * @returns any tipinde değer.
 */
export const Tgeo: any;

/**
 * Tgeo: Bu sınıf runtime'da CLASS(...) ile sarılmıştır.
 * Genelde sahnedeki/uygulamadaki belirli bir varlık tipini (T* sınıfları) temsil eder.
 * Tanımlandığı modül: geo
 */
export class Tgeo {
  /**
     * Tgeo kurucu metodu.
     * Bu sınıfın yeni bir örneğini oluşturur ve iç durumunu başlatır.
     * @returns Yeni oluşturulmuş Tgeo örneği.
     * @example
     *   new Tgeo();
     */
  constructor();
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param Number_isFinite_nv_____nv___0__this_EPS Number_isFinite_nv_____nv___0__this_EPS parametresi.
     * @param nv_______return_this_EPS_____________ nv_______return_this_EPS_____________ parametresi.
     * @param b b parametresi.
     * @param eps eps parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(Number_isFinite_nv_____nv___0__this_EPS, nv_______return_this_EPS_____________, b, eps);
     */
  if(Number_isFinite_nv_____nv___0__this_EPS: any, nv_______return_this_EPS_____________: any, b: any, eps: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param r____r____r__ r____r____r__ parametresi.
     * @param y__ y__ parametresi.
     * @param y y parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(r____r____r__, y__, y);
     */
  if(r____r____r__: any, y__: any, y: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param this_approxZero_rxs_ this_approxZero_rxs_ parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(this_approxZero_rxs_);
     */
  if(this_approxZero_rxs_: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param _res_hit__return___hit_false _res_hit__return___hit_false parametresi.
     * @param parallel_res_parallel________const__t parallel_res_parallel________const__t parametresi.
     * @param u u parametresi.
     * @param point_ point_ parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(_res_hit__return___hit_false, parallel_res_parallel________const__t, u, point_);
     */
  if(_res_hit__return___hit_false: any, parallel_res_parallel________const__t: any, u: any, point_: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param this_approxZero_abLenSq___return___proj_a_slice__ this_approxZero_abLenSq___return___proj_a_slice__ parametresi.
     * @param t_0 t_0 parametresi.
     * @param clamp_true________let_t clamp_true________let_t parametresi.
     * @param ab____abLenSq______const_clamped________t ab____abLenSq______const_clamped________t parametresi.
     * @param 0 0 parametresi.
     * @param 1_______return___proj__a_0__ab_0__t 1_______return___proj__a_0__ab_0__t parametresi.
     * @param a_1__ab_1__t_ a_1__ab_1__t_ parametresi.
     * @param t t parametresi.
     * @param clamp_clamped_____________ clamp_clamped_____________ parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(this_approxZero_abLenSq___return___proj_a_slice__, t_0, clamp_true________let_t, ab____abLenSq______const_clamped________t, 0, 1_______return___proj__a_0__ab_0__t, a_1__ab_1__t_, t, clamp_clamped_____________);
     */
  if(this_approxZero_abLenSq___return___proj_a_slice__: any, t_0: any, clamp_true________let_t: any, ab____abLenSq______const_clamped________t: any, 0: any, 1_______return___proj__a_0__ab_0__t: any, a_1__ab_1__t_: any, t: any, clamp_clamped_____________: any): any;
  /**
     * for metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param const_p_of_pts const_p_of_pts parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.for(const_p_of_pts);
     */
  for(const_p_of_pts: any): any;
  /**
     * for metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param let_i let_i parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.for(let_i);
     */
  for(let_i: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param this_approxZero_A_ this_approxZero_A_ parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(this_approxZero_A_);
     */
  if(this_approxZero_A_: any): any;
  /**
     * for metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param let_i let_i parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.for(let_i);
     */
  for(let_i: any): any;
  /**
     * for metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param let_i let_i parametresi.
     * @param j j parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.for(let_i, j);
     */
  for(let_i: any, j: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param intersect__inside__ intersect__inside__ parametresi.
     * @param y y parametresi.
     * @param w w parametresi.
     * @param h h parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(intersect__inside__, y, w, h);
     */
  if(intersect__inside__: any, y: any, w: any, h: any): any;
  /**
     * if metodu.
     * Bu metod Tgeo örneğinin davranışının bir parçasıdır.
     * @param this_approxZero_det___return_null______const_id this_approxZero_det___return_null______const_id parametresi.
     * @param b___M_b_id b___M_b_id parametresi.
     * @param c___M_c_id c___M_c_id parametresi.
     * @param d___M_a_id d___M_a_id parametresi.
     * @param tx___M_c_M_ty___M_d_M_tx__id tx___M_c_M_ty___M_d_M_tx__id parametresi.
     * @param ty___M_b_M_tx___M_a_M_ty__id______________static_mat2dApplyToPoint_M ty___M_b_M_tx___M_a_M_ty__id______________static_mat2dApplyToPoint_M parametresi.
     * @param p p parametresi.
     * @returns İşlevin döndürdüğü değer.
     * @example
     *   instance.if(this_approxZero_det___return_null______const_id, b___M_b_id, c___M_c_id, d___M_a_id, tx___M_c_M_ty___M_d_M_tx__id, ty___M_b_M_tx___M_a_M_ty__id______________static_mat2dApplyToPoint_M, p);
     */
  if(this_approxZero_det___return_null______const_id: any, b___M_b_id: any, c___M_c_id: any, d___M_a_id: any, tx___M_c_M_ty___M_d_M_tx__id: any, ty___M_b_M_tx___M_a_M_ty__id______________static_mat2dApplyToPoint_M: any, p: any): any;
}

