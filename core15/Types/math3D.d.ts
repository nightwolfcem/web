// math3D.js — detaylı .d.ts (manuel stil)
// Kaynak: core12/math3D.js

/**
 * EPS: math3D modülü içinde tanımlı let değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math3D
 * @internal
 * @returns number tipinde değer.
 */
export const EPS: number;

/**
 * PI: math3D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math3D
 * @internal
 * @returns any tipinde değer.
 */
export const PI: any;

/**
 * TAU: math3D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math3D
 * @internal
 * @returns any tipinde değer.
 */
export const TAU: any;

/**
 * math3D: math3D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math3D
 * @returns any tipinde değer.
 */
export const math3D: any;

/**
 * Math3D: math3D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math3D
 * @returns any tipinde değer.
 */
export const Math3D: any;

