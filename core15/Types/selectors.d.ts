/** CSS selector helpers used across modules */
export const selectors: {
  root: string;        // root container selector
  content: string;     // content layer selector
  overlay: string;     // overlay layer selector
  selection: string;   // selection layer selector
  elementIdAttr: string; // attribute name for element id (e.g., data-id)
};
