// math2D.js — detaylı .d.ts (manuel stil)
// Kaynak: core12/math2D.js

/**
 * EPS: math2D modülü içinde tanımlı let değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @internal
 * @returns number tipinde değer.
 */
export const EPS: number;

/**
 * PI: math2D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @internal
 * @returns any tipinde değer.
 */
export const PI: any;

/**
 * TAU: math2D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @internal
 * @returns any tipinde değer.
 */
export const TAU: any;

/**
 * _clamp: math2D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @internal
 * @returns Function tipinde değer.
 */
export const _clamp: Function;

/**
 * math2D: math2D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @returns any tipinde değer.
 */
export const math2D: any;

/**
 * Math2D: math2D modülü içinde tanımlı const değeridir.
 *
 * Kullanım senaryosu: Bu değer modülün çalışma zamanındaki davranışını/ayarlarını tutar veya yardımcı veri olarak kullanılır.
 * Tanımlandığı modül: math2D
 * @returns any tipinde değer.
 */
export const Math2D: any;

/**
 * _clamp: _clamp bu modül içinde yardımcı bir işleve hizmet eder.
 *
 * Kullanım senaryosu: _clamp tipik olarak bu modülün akışı içinde çağrılır.
 * Tanımlandığı modül: math2D
 *
 * @param x x parametresi.
 * @param min min parametresi.
 * @param max max parametresi.
 * @returns any
 * @example
 *   _clamp(x, min, max);
 */
export function _clamp(x: any, min: any, max: any): any;

